import React from "react";

const Header = () => (
  <header className="app-header">
    <a href="/">
      <span className="">Request Tracker</span>
      <img alt="Request Tracker" />
    </a>
  </header>
);

export default Header;
